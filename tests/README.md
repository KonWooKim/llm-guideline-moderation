Run the test suite with:

```bash
python -m unittest discover -s tests -p "test_*.py"
```

If your environment does not have `python` on PATH, use the interpreter path directly after installing Python 3.10+.
